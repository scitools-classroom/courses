
# Iris course sample answer 4.1b
# make a colour blockplot of "airtemps"

try:
    qplt.pcolormesh(airtemps)
    plt.show()
except Exception as err:
    print('Error occurred:\n  "{}"'.format(err))

print("NOTE: the 'qplt.pcolormesh' method requires a 2d cube.")